Here’s a simple **Task Automation script in Python** that matches your scope. I’ll show you all three options so you can pick the one you want to run:

---

### 🖼️ Option 1: Move all `.jpg` files to a new folder
```python
import os
import shutil

def move_jpg_files(source_folder, destination_folder):
    if not os.path.exists(destination_folder):
        os.makedirs(destination_folder)

    for file in os.listdir(source_folder):
        if file.lower().endswith(".jpg"):
            shutil.move(os.path.join(source_folder, file),
                        os.path.join(destination_folder, file))
            print(f"Moved: {file}")

# Example usage
move_jpg_files("C:/Users/YourName/Pictures", "C:/Users/YourName/JPG_Files")
```

---

### 📧 Option 2: Extract all email addresses from a `.txt` file
```python
import re

def extract_emails(input_file, output_file):
    with open(input_file, "r") as f:
        text = f.read()

    emails = re.findall(r"[a-zA-Z0-9._%+-]+@[a-zA-Z0-9.-]+\.[a-z]{2,}", text)

    with open(output_file, "w") as f:
        for email in emails:
            f.write(email + "\n")

    print(f"Extracted {len(emails)} emails to {output_file}")

# Example usage
extract_emails("sample.txt", "emails.txt")
```

---

### 🌐 Option 3: Scrape the title of a fixed webpage
```python
import requests
from bs4 import BeautifulSoup

def scrape_title(url, output_file):
    response = requests.get(url)
    soup = BeautifulSoup(response.text, "html.parser")
    title = soup.title.string

    with open(output_file, "w") as f:
        f.write("Page Title: " + title)

    print(f"Title saved to {output_file}: {title}")

# Example usage
scrape_title("https://example.com", "title.txt").
